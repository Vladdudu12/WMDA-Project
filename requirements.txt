requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
tenacity>=8.2.0          # retry logic with backoff
fake-useragent>=1.4.0    # rotate user agents
python-dotenv>=1.0.0     # for API keys later
tqdm>=4.66.0             # progress bars
pandas>=2.1.0
numpy>=1.26.0
# ML/analysis (later)
scikit-learn>=1.3.0
nltk>=3.8
matplotlib>=3.8.0
jupyter>=1.0.0

nltk>=3.8                    # tokenization, stopwords, VADER sentiment
scikit-learn>=1.3.0          # already there
sentence-transformers>=2.7   # embeddings (downloads MiniLM model)
bertopic>=0.16               # topic modeling on top of embeddings
umap-learn>=0.5              # dimensionality reduction (BERTopic dep)
hdbscan>=0.8                 # clustering (BERTopic dep)
matplotlib>=3.8              # already there
seaborn>=0.13                # nicer charts
wordcloud>=1.9               # for the obligatory word clouds
plotly>=5.18                 # interactive charts for the writeup